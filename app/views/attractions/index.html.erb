
<% if !@user.admin %>
  <% @attractions.each do |attraction| %>
  <%= link_to "Go on #{attraction.name}", attraction_path(attraction.id) %>
  <% end %>
<% end %>




<% if @user.admin%>
  <% @attractions.each do |attraction| %>
  <%= link_to "Show #{attraction.name}", attraction_path(attraction.id) %>
  <% end %>

  <%= link_to "New Attraction", new_attractions_path %>
<% end %>
